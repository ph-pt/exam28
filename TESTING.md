# TESTING — 検収チェックリスト(spec_quest_v4_1.md §9)

最終実行: 2026-07-26 / 実装: Claude Code
対象: v4.1(スキン科目別5枠 / 採用規則の等値判定 / 予習サイクル交代の起動時確認)

---

## 0. 実行方法

### ビルド

```bash
node build.mjs
```

`banks/*.json` と `skins/*.json` を `src/index.template.html` の10マーカーへ、
`src/engine/*.js` + `src/ui/*.js` を `APP_JS` マーカーへ注入し、`dist/` を生成します。
依存パッケージはありません(Node 20+)。

生成物: `dist/index.html`(単一HTML)/ `manifest.json` / `sw.js` / `icon-{180,192,512}.png` / `.nojekyll`

### 自動テスト

```bash
node test/run-tests.mjs
```

`src/engine/*.js` を dist と同じ順序で連結し `node:vm` 上で評価します。
ビルド成果物と同一のコードを対象にするため、注入順のズレも検出できます。

**最新結果: 221件中 221件成功 / 失敗 0**

> **期待値はバンクの中身から導出しています。** 問題数・回番号・cat名を直書きしないため、
> 月次のバンク更新や予習サイクルの交代で検収テストが落ちてデプロイが止まることはありません。
> (旧 `Y_20260718`(36問・回37/38)と現行 `Y_20260728`(52問・回1〜6)の両方で221件全成功を確認済み)

### ローカルでの手動確認

`dist/index.html` をブラウザで直接開けば file:// でも全機能が動きます(Service Worker を除く)。
Service Worker・PWA・オフラインを確認するときは HTTP で配信してください:

```bash
node -e "const{createServer}=require('http'),{readFile}=require('fs/promises'),{join,extname}=require('path');const T={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.png':'image/png'};createServer(async(q,s)=>{let p=new URL(q.url,'http://x').pathname;if(p==='/')p='/index.html';try{const b=await readFile(join('dist',p));s.writeHead(200,{'content-type':T[extname(p)]||'application/octet-stream','cache-control':'no-cache'});s.end(b)}catch(e){s.writeHead(404);s.end('404')}}).listen(8765,()=>console.log('http://localhost:8765/'))"
```

---

## 1. §9 チェックリスト 判定一覧

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| 1 | タブ間で進捗・XP・演出が漏れない / 3科+国語が自科目スキンを参照 | ✅ 自動+手動 | §2.1 / §3.1 |
| 2 | 解答確定ごとに `quest_state_*` が更新される | ✅ 手動 | §3.2 |
| 3 | 期日計算が問題別 `last` 基準(3/7/21・1/3、箱3含む) | ✅ 自動 | §2.3 |
| 4 | セーブコード復元時のみ `last` を一括初期化 | ✅ 自動 | §2.4 |
| 5 | localStorage不可環境でバナー+コード導線 | ✅ 自動+手動 | §2.5 / §3.5 |
| 6 | bank_K空でドア非表示 → 投入でドア出現+Kスキン / Kのans例外 | ✅ 自動+手動 | §2.6 / §3.4 |
| 7 | §3.1 採用規則(投入の維持・通知付き切替・内蔵に戻す) | ✅ 自動+手動 | §2.7 / §3.6 |
| 8 | §7.1 サイクル交代(検出→確認→承諾/拒否→記録・リセット) | ✅ 手動 | §3.3 |
| 9 | チェックサム統一後、全科目でセーブコード往復 | ✅ 自動 | §2.2 |
| 10 | file:// で全機能動作(SW以外) | ✅ 手動 | §3.7 |
| 11 | ホーム画面追加後オフラインで起動・プレイ | ⚠️ 一部手動未実施 | §3.8 |
| 12 | push→Pages反映→更新トースト | ⚠️ 一部手動未実施 | §3.9 |

⚠️ の2件は**実機(iPad)と実際のpushが必要な部分のみ未実施**です。詳細は各節に記載しています。
アプリ側の仕組み(SW登録・プリキャッシュ・オフライン起動・更新トースト・タップで適用)は
ローカルHTTPで動作確認済みです。

### 継承チェック

| 出典 | 項目 | 判定 |
|---|---|---|
| fukushu §10 | V2コード往復 / V1互換読み込み / バンク成長後の旧コード復元 / 巻き戻し検出 / バリデーション拒否4ケース | ✅ 全項目自動 |
| yoshu §11 | 12項目(HP部分点・封印/討伐・圧縮ライトナー・解放ゲート・交代フロー・バリデーション・コード・「あと◯日」非表示・間口純粋化4件) | ✅ 全項目自動(演出発火はUI手動) |

---

## 2. 自動テストの内訳(node test/run-tests.mjs)

### 2.1 §9-1 科目ごとの世界の分離
- 4科の `skinVersion` がすべて異なる
- 算数=日本一周47ノード / 理科=研究所たんけん16ノード / 社会=日本一周(社会版)47ノード / 国語=ことばの旅20ノード
- 4科の称号セットがすべて異なる / 出発地が異なる
- Kスキンの `writeTip` が §5.1 指定どおり「漢字はノートにも書いてみよう」
- ボススキンは `map` を持たない(yoshu §3)
- 全スキンに `hintPraise` がある(ヒント称賛文化の必須要件)
- 称号 `cond` は実装済み条件名のみ(noHintStreakN / hintCorrectN / revengeWin / perfectStage / totalCorrectN)
- `map.nodes` は60以下

### 2.2 §9-9 セーブコード
- M/R/S/K/Y **全科目**で V2 往復(箱・xp が完全一致)。チェックサム統一の実証
- V1 コードの互換読み込み(箱・xp が復元される)
- バンク成長後の旧コード復元: 増えた5問が復習では箱2、予習ではキー未設定(未出題)
- 巻き戻し検出: コードの問題数 > 現バンク → `bank_old` エラー
- 予習の旧サイクルコードが問題数不一致でエラーになる(yoshu §7)
- 不正入力5種を棄却 / チェックサム改ざんを棄却

### 2.3 §9-3 期日計算
- `INTERVALS.fukushu` = 0/3/7/21日、`INTERVALS.yoshu` = 0/1/3/3日
- 境界値(2日/3日、6日/7日、20日/21日、予習の1日・3日)で到来判定が正しい
- **箱3が21日で必ず復習候補に含まれる**(shallow retention対策・fukushu v2.1の必須挙動)
- `last` が無い問題は期日計算の対象外(未出題プールへ)

### 2.4 §9-4 `last` の初期化タイミング
- `applyRestoredCode` で全問の `last` が復元日に揃う(コード復元時のみ)
- 通常の解答では解いた問題の `last` だけが動く
- 箱遷移: 復習の未出題(箱2)→正解→箱3 / 予習の未出題→初正解→箱1 / 誤答→箱0

### 2.5 §9-5 localStorage不可環境
- 書き込み不可環境で `storageAvailable=false`、`saveState` が false を返す
- `storageWriteFailed` が立つ(常時バナーの根拠)
- 読めなくても初期stateで起動できる / 保存不可でもセーブコードは発行できる

### 2.6 §9-6 国語(K)枠
- 初期 `bank_K` は空バンク(0問)で、構造チェックを通る=起動できる
- K は ans 1種のみでも受理される(読み=ひらがな / 書き=漢字)
- 同じ形を M で投入すると拒否される(例外がKに限定されている)

### 2.7 §9-7 §3.1 採用規則(等値判定)
- 規則1: 投入なし → 内蔵
- 規則2: 内蔵versionが記録と一致 → 投入を採用。**再起動をまたいで維持**
- 規則3: pushで内蔵が変わった → 内蔵へ切替 + `switched` 通知 + 投入解除(**無言の巻き戻りをしない**)
- 「内蔵に戻す」で内蔵へ戻る / 壊れたキャッシュは内蔵へフォールバック+警告
- **bank_Y は自動切替せず `pendingSwitch` を返す**(§7.1へ回す)。拒否できるよう旧バンクのミラーが残る

### 2.8 §4.2 state の突合
- 現バンクにないidは持ち越さない / 増えた問題は未出題(キーなし)
- `bankVersion` が現バンクに更新され、xp は保持される / 保存→読み戻しが一致

### 2.9 yoshu §11-1 HP・マスター率・封印
- 全問未出題 → 進捗率0 / HP100
- 全問が箱1 → 進捗率50(部分点)だがマスター率は0
- 全問が箱2以上 → HP0 かつマスター率100(討伐判定と矛盾しない)
- 箱0はダメージにならない
- **36問中8問を箱1にすると HP89**(yoshu §4 の挙動目安に一致。合成36問データで検証するのでバンクの問題数に依存しない)
- `SEAL_THRESHOLD` = 80

### 2.10 yoshu §11-4 解放ゲート
- 回がバンクの `kai` から自動検出され、昇順に並ぶ(期待値はバンクから導出)
- 0問の回は無視される(現行バンクでは国語に回5がないため、チップは 回1/2/3/4/6 になる)
- 初期は各科目の最初の回のみ解放
- 自己修復①: リセット後も各科目の最初の回は解放
- 自己修復②: 箱に進捗がある回は解放済みとみなす
- 未解放の回の問題は母数から外れる / 全回解放フラグが討伐ガードの条件になる

### 2.11 yoshu §11-5 討伐記録
- 解放し忘れの回があると **銀(取り逃がし)**。mastery は全問題ベース
- 全問マスターなら **金(討伐)** / mastery 100%
- ボス名・テスト日が記録され、追記される

### 2.12 yoshu §11-9〜12 間口の純粋化(v1.3.1)
- **200回試行(4科×50)で他科目の混入0件**
- 解放済みが8問未満の科目でセッションが短縮される(他科目で埋めない)
- 🎲おまかせミックスは4科すべてから8問
- **全問が箱0の状態でも間口内に限定される**(箱0プールのcat絞り込み)
- 未解放の回の問題は出題されない

### 2.13 fukushu §4 復習のセッション構成
- 初回は8問
- **箱0は間口に関係なく混入し、間口ありでは最大2問**(v4.1 記録4で承認済の据え置き)
- ミックスでは箱0が優先充当される
- `_revenge` フラグが箱0の問題にのみ付く
- `last` が無くても8問組める(未出題プールから補完)

### 2.14 fukushu §6 入力モード・正規化
- 数値/分数/小数 → キーパッド、テキスト → テキスト入力
- 全角数字・全角スラッシュ・単位つき入力を受理
- カタカナ→ひらがな折り畳み、かな交じり入力(「すいさんかカルシウム」)を受理
- 不一致は正しく弾く

### 2.15 バリデーション拒否ケース
復習(M/R/S/K): subject不一致 / 既存id欠落 / id重複 / テキストans1種 / catsに未定義のcat / id形式不正 / 新規idが続き番号でない。retiredでの残置は受理。
予習(Y): subject不一致 / cat欠落 / catsに4科が揃わない / testDateなし / 国語以外のans1種。国語の漢字問題のans1種は受理。既存idの欠落は検査しない(サイクル使い捨て)。

### 2.16 ビルド成果物
- マーカー10ブロックがすべて存在(§1)/ `APP_JS` は置換済み
- バンク・Kスキンが実際に注入されている
- **「あと◯日」表現がどこにも存在しない**(yoshu §11-8)。`testDate` は討伐記録用のみ
- 実行時 `fetch` をしない / 外部スクリプト・外部URL参照がない(file://互換)
- file:// では SW を登録しないガードがある

---

## 3. 手動確認の記録(2026-07-26 実施)

以下はブラウザ上で実際に操作して確認した結果です。再確認したい場合は同じ手順を辿ってください。

### 3.1 §9-1 タブ・科目の分離
1. 起動 → 🗾ふくしゅう の科目ドアに 算数 / 理科 / 社会 が並ぶ(国語は非表示)
2. 各ドアに自科目の旅マップの現在地が出る
   - 算数「さんすうクエスト 日本一周 / 🐄 北海道(0/46)」
   - 理科「りかクエスト 研究所たんけん / 🚪 じっけん室にゅうこう(0/15)」
   - 社会「しゃかいクエスト 日本一周 / 🐄 北海道(0/46)」
3. 算数で1問正解 → 👑じゅんび へ切替 → **ボスHPは100のまま、封印はすべて0%**
   → 復習の進捗が予習側に漏れないことを確認
4. 国語で1問正解後、`quest_state_K.xp=1` / `quest_state_M.xp=0` → XPが科目別に独立

**結果: 合格**

### 3.2 §9-2 自動保存
1. 算数 →「きほん」→ 1問目に 80 と入力 →「こたえあわせ」
2. `quest_state_M` = `{"v":1,"bankVersion":"M_2026_07_02b","boxes":{"M0001":3},"last":{"M0001":"2026-07-26"},"xp":1}`
   → **解答確定の時点で書き込まれている**(結果画面を待たない)
3. リロード → 前回の科目画面が復元され、マップが1歩進んだ状態(ことばの村 → ひらがな野原、1/19)

**結果: 合格**

### 3.3 §9-8 §7.1 予習サイクル交代フロー
`quest_bank_Y` の `builtinVersion` を古い値に書き換えて「pushで新しい内蔵bank_Yが届いた」状態を作る。

> この記録は `Y_20260718`(36問・回37/38・28/29)で実施したものです。
> 現行バンクは `Y_20260728`(52問・回1〜6)なので、再現時の回番号は読み替えてください。
> フロー自体はバンクの中身に依存しません。

| 手順 | 期待 | 結果 |
|---|---|---|
| リロード | 起動時に交代確認画面「⚠️ 新しいボスが あらわれた!」が出る | ✅ |
| 「まだ やめておく」 | 旧バンクで継続。討伐記録は書かれない | ✅ `quest_boss_log_Y` = null |
| もう一度リロード | **再度確認画面が出る**(§7.1-3) | ✅ |
| 回チップを1回タップ | 「🗝️ 習ったら もういちどタップ!」に変わる。まだ解放されない | ✅ `quest_unlock_Y` = null |
| もう一度タップ | 解放され、ボスが powerUp セリフを宣言 | ✅ `["算数:38"]` 保存 / 既定文が表示 |
| リロード →「うん、挑む!」 | (a)討伐記録追記 (b)state・unlockリセット (c)新ボス登場演出 | ✅ 下記 |

承諾後の状態:
- 討伐記録: `{"bossName":"バイバイ大魔王","emoji":"😈","testDate":"2026-07-18","result":"取り逃がし","mastery":0}` → **銀バッジ・全問題ベースmastery**
- `quest_state_Y` がリセット / `quest_unlock_Y` がクリア(回38が🔒に戻る)
- ボスのセリフが `appear`「ふはは! 7がつ18にち、われはてこでも動かんぞ!」に
- ミラーの `builtinVersion` が最新に更新 → 次回起動で再確認されない

**結果: 合格**。おうちの人メニュー経由の交代も同じ `proceedCycleSwitch()` を通ります(記録漏れ防止)。

> 金バッジ(討伐)側の分岐は自動テスト §2.11 で確認しています。

### 3.4 §9-6 国語(K)枠
1. 初期状態: 科目ドアは 算数/理科/社会 の3つ(**国語ドアなし**)
2. おうちの人メニュー → バンク枠で「国語(K)」を選択 → K0001〜K0003 のバンクを貼り付け → 更新
   - K0001 は ans がひらがな1種、K0002 は漢字1種 → **拒否されずに受理される**
3. 戻る → **国語ドアが出現**「📖 国語 / こくごクエスト ことばの旅 / 🏡 ことばの村(0/19)」
4. 国語を開く → Kスキンが適用(タイトル・マップ・アクセント色 `#C86FA0`・writeTip「漢字はノートにも書いてみよう」)
5. 「漢字」間口で出題 → 漢字書き問題が**テキスト入力モード**になり、「性格」で正解

**結果: 合格**

### 3.5 §9-5 localStorage不可環境
`localStorage.setItem` が例外を投げる状態にして再描画。

- 画面上部に「⚠️ 保存できない環境です。終わったらセーブコードをメモしてね」が常時表示
- バナー内の「🔑 セーブコードを出す」からセーブコード画面へ遷移し、`V2-260726-…` が発行される

**結果: 合格**

### 3.6 §9-7 採用規則
- おうちの人メニューでKバンクを投入 → リロード後も投入バンクが有効(規則2)
- 内蔵版が変わった場合の切替+トーストは自動テスト §2.7 で確認
- 「えらんだ科目を内蔵バンクに戻す」導線あり

**結果: 合格**

### 3.7 §9-10 file:// 互換
`dist/index.html` を直接開く。

- 起動・科目ドア・マップ・出題・キーパッド・判定・自動保存(localStorage)すべて動作
- コンソールエラーなし
- Service Worker は `location.protocol` ガードにより登録されない(仕様どおり)

**結果: 合格**

### 3.8 §9-11 オフライン
ローカルHTTP(`http://localhost:8765/`)で起動 → SW登録を確認 → **サーバを停止** → リロード。

- SW登録: scope `/`、active、キャッシュ名 `quest-<内容ハッシュ>`
- プリキャッシュ: `/`, `/index.html`, `/manifest.json`, `/icon-192.png`, `/icon-512.png`, `/icon-180.png`
- サーバ停止後もリロードで**完全に起動**し、前回の画面が復元される
- オフラインのまま出題を開始でき、解答・自動保存も動作

**結果: オフライン起動・プレイは合格。**

⚠️ **未実施**: iPad Safari で「ホーム画面に追加」した状態での起動確認。
下記の手順で確認してください(Pages公開後):

1. iPad Safari で `https://ph-pt.github.io/exam28/` を開く
2. 共有 → 「ホーム画面に追加」
3. **機内モードをON** にしてホーム画面のアイコンから起動
4. 起動すること・タブ切替・出題・解答・自動保存が動くことを確認
5. standalone表示(Safariのアドレスバーが出ない)になっていることを確認

### 3.9 §9-12 push → Pages → 更新トースト
アプリ側の更新検知は、`dist/sw.js` のキャッシュ名を書き換えて新デプロイを模擬し確認済み。

- 新しい SW を検知 → トースト「あたらしいバージョンがあるよ(タップで読みこみ)」を表示
- **学習中に勝手にリロードしない**(タップするまで待つ)
- タップ → 進捗を書き出し → SKIP_WAITING → controllerchange → 1回だけリロード
- 古いキャッシュが削除され、新しいキャッシュのみが残る

**結果: アプリ側の更新フローは合格。**

⚠️ **未実施**: 実際の push → GitHub Actions → Pages 反映までの一連。
下記の手順で確認してください:

1. リポジトリの Settings → Pages で source が **GitHub Actions** になっていることを確認
2. main に push
3. Actions タブで `build & deploy to Pages` が成功すること(build → 自動テスト → deploy)
4. `https://ph-pt.github.io/exam28/` が更新されること
5. すでに開いていた端末で数十秒待つ or 再訪 → 更新トーストが出て、タップで新版になること

> ワークフローは build の直後に `node test/run-tests.mjs` を実行します。
> §9 の自動テストが落ちるとデプロイされません。

---

## 4. 既知の実装メモ

- **テキスト正規化の全角/半角**: 旧2エンジンで除去対象が食い違っていた(復習=`！？`、予習=`!?`)ため、
  fukushu §6「空白・句読点を除去」に従い**両方を除去**する形に統一しました。
  従来正解だった入力が不正解になることはありません。
- **`powerUp` セリフ**: 現行の `skin_boss.json` には `powerUp` が未定義のため、
  回の解放時はエンジン既定文が出ます(yoshu §3「未指定ならエンジン既定文」どおり)。
- **復習の間口補完**: 間口内の問題が8問に満たない場合、復習側は全体から補完します
  (fukushu §4「不足時のみ全体から」)。予習側は補完せず短縮します(yoshu §6 v1.3.1)。
  タブごとに挙動が異なるのは v4.1 記録4 の承認済み据え置きです。
- **アイコン**: `build.mjs` が `node:zlib` だけで PNG を生成します(外部依存を持たないため)。
  オレンジの角丸に白い星のマークです。差し替える場合は `makeIconPng()` を編集してください。
- **検収テストとバンクの分離**: 自動テストは問題数・回番号・cat名を直書きせず、
  `banks/*.json` から期待値を導出します。バンクを更新しただけでCIが赤くなり
  デプロイが止まる、という事態を避けるためです。
  逆に「仕様が定めた値」(復習3/7/21日・予習1/3日、封印80%、`INTERVALS`、
  yoshu §4 の36問時HP89 など)は合成データで直接検証しています。
- **ボススキンとバンクの対応**: スキンとバンクは独立した枠なので、
  サイクル交代でバンクだけを差し替えるとボスの見た目・セリフは前サイクルのまま残ります。
  yoshu §9 の運用フローどおり、バンクと一緒に `skin_boss` も更新してください。

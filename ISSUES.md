# ISSUES — 仕様差し戻し事項

宛先: メタ管理チャット(Fable)/ 起票: Claude Code(実装)

spec_quest_v4_1.md §11 手順7「新たに仕様変更が必要と判断した箇所は実装せず、ISSUES.md に追記して停止する」
および §10「エンジン実装で仕様変更が必要になったらClaude Codeは実装で解決せずメタ管理チャットに差し戻す」に基づく起票簿。

---

## 現在オープンな課題

**なし。** v4.1 実装時点(2026-07-26)で、実装を止める仕様上の未決事項はありません。

§9 の全12項目の確認結果は [TESTING.md](TESTING.md) を参照してください。

---

## クローズ済み

### 課題1 ふくしゅうスキンの枠が1つしかない(v4.0 §1 と §2 の内部矛盾)
- **起票**: 2026-07-26。既存3科が別々の演出世界(M=日本一周47ノード / R=研究所たんけん16ノード / S=日本一周社会版47ノード、writeTipも3科で別)であるのに対し、v4.0 §1 はふくしゅうスキンを1枠・マーカー7個と定めていた。1本に統合すれば既存資産の削除、増やせば §1 の改訂になるため実装判断の範囲外と判断。
- **裁定**: v4.1 §1 で **案A(科目別5枠・マーカー10個)** を採用。`skin_fukushu_{M,R,S,K}.json` + `skin_boss.json`。§2 に「ふくしゅう各科目は自科目のスキンのみを参照する」を明記。
- **対応**: 抽出済みの M/R/S をそのまま採用。K は §5.1 に従い「こくごクエスト ことばの旅」(journey型20ノード)を新規生成。
- **検証**: TESTING.md §2.1 / §3.1 / §3.4。**クローズ**

### 課題2 bankVersion の新旧比較規則が未定義(v4.0 §1)
- **起票**: 2026-07-26。「bankVersionが新しい方を採用」とあるが値の書式が科目ごとに揺れており(`M_2026_07_02b` / `Y_20260718`)、順序判定を誤ると投入バンクの無言の巻き戻り、または最新バンクの永久無視という無警告のデータ損失を招くため、推測実装は不可と判断。
- **裁定**: v4.1 §3.1 で **順序判定を廃止し等値判定のみ** に確定。投入時点の内蔵versionを併記して保存し、一致すれば投入を維持、変化していれば内蔵へ切替+トースト通知。
- **対応**: `src/engine/adopt.js` に実装。バンク・スキン共通。
- **検証**: TESTING.md §2.7 / §3.6。**クローズ**

### 課題3 ビルド内蔵バンクによる予習サイクル交代の扱いが未定義(v4.0 §1 × yoshu §4/§7)
- **起票**: 2026-07-26。push経由で新サイクルの bank_Y が届くと、確認ダイアログを出す主体がおらず、**討伐記録(金/銀バッジ)が追記されないままボスが入れ替わる**。復元不能なため推測実装は不可と判断。
- **裁定**: v4.1 §7.1 で **案A(起動時確認フロー)** を採用。検出→確認→承諾で (a)討伐記録追記 (b)state・unlockリセット (c)新バンク採用・新ボス演出。拒否時は旧バンクで継続し次回起動時に再確認。現在有効な bank_Y は常に `quest_bank_Y` にミラー。
- **対応**: `src/ui/junbi.js` の `proceedCycleSwitch()` に集約。おうちの人メニュー経由の交代も同じ関数を通す(記録漏れ防止)。
- **検証**: TESTING.md §3.3(承諾・拒否・再確認・記録内容をすべて実機操作で確認)。**クローズ**

### 記録1〜4(実装判断)
v4.0 実装時に「v4優先ルールで一意に決まるが既存挙動が変わる」ものとして起票し、
**v4.1 §10 で4件とも承認済み**。実装に反映しています。

| # | 内容 | 反映先 |
|---|---|---|
| 1 | セーブコードのチェックサム算法を全科目 fukushu 版に統一 | `src/engine/savecode.js` |
| 2 | セーブコードを結果画面から外し、おうちの人メニューへ | `src/ui/parent.js` / `src/ui/quiz.js` |
| 3 | 箱の初期値は科目系統ごとに据え置き(M/R/S/K=箱2、Y=未出題別管理) | `src/engine/leitner.js` / `savecode.js` |
| 4 | 箱0の間口またぎは科目系統ごとに据え置き | `src/engine/leitner.js` |

---

## 補足: 実装判断で解決した軽微な差異(差し戻し不要)

仕様の範囲内で一意に決まったが、旧2エンジンで実装が食い違っていたため記録します。
異議があれば指摘してください。詳細は TESTING.md §4。

1. **テキスト正規化の句読点**: 復習エンジンは全角 `！？`、予習エンジンは半角 `!?` を除去していた。
   fukushu §6「空白・句読点を除去」に従い両方を除去する形に統一。従来正解だった入力が不正解になることはない。
2. **`skin_boss.json` に `powerUp` が未定義**: yoshu §3「未指定ならエンジン既定文」に従い既定文を使用。
   スキン差し替え時に追加されればそちらが使われる。
3. **アイコン画像**: v4.1 §7 は「名前・アイコン・standalone表示」とのみ規定。
   外部依存を持ち込まないため `build.mjs` が `node:zlib` だけで PNG を生成する方式にした
   (オレンジの角丸+白い星)。デザイン指定があれば差し替える。
